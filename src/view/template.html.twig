<!DOCTYPE HTML>
<!-- Couleurs :
    Blanc       #FFFFFF
    Gris        #333
    Gris clair  #919191
    Gris foncé  #181A1B
    Orangé      #542E00
    Orang Clair #F08400
    Bleu clair  #00A3D0
    Bleu gris   #38435A
    Bleu foncé  #181D27
    -->
<!-- Blog d'écrivain - Made by Lacrotte Florent / Projet 4 - OpenClassrooms -->

<html lang="fr">
    <head>
        {% include "include_head.html" %}
        {% block head %}{% endblock %}
    </head>
    <body>
    {% block body %}{% endblock %}
        <div class="container-fluid blockToast">
            {% for flashMessage in FLASHBAG %}
                <div class="toast" role="alert" aria-live="assertive" aria-atomic="true" data-autohide="false">
                    <div class="toast-header">
                        <svg class="bd-placeholder-img rounded mr-2" width="20" height="20" xmlns="http://www.w3.org/2000/svg" preserveAspectRatio="xMidYMid slice" focusable="false" role="img">
                            <rect width="100%" height="100%" fill="{{flashMessage.type}}"></rect>
                        </svg>
                        <strong class="mr-auto">Informations</strong>
                        <small class="ml-3">message n°{{loop.index}}</small>
                        <button type="button" class="ml-2 mb-1 close" data-dismiss="toast" aria-label="Close">
                            <span aria-hidden="true">&times;</span>
                        </button>
                    </div>
                    <div class="toast-body">
                        {{flashMessage.message}}
                    </div>
                </div>
            {% endfor %}
        </div>
        {% if SESSION.login != empty %}
            <div id="connect" class="container-fluid">
                <div class="row">
                    <div class="d-flex flex-column align-items-end w-100">
                        <div class="connect d-flex flex-column align-items-end">
                        <a href="/disconnectAdminConnexion" class="badge badge-dark p-3 rounded-0">Déconnexion</a>
                        <span class="badge badge-success p-3 rounded-0 d-none d-lg-block w-50 "><i class="fas fa-power-off"></i></span>
                        </div>
                    </div>
                </div>
            </div>
        {% endif %}
        {% if SESSION.cookies == empty %}
            <div id="cookiesConscent" class="container-fluid animated fadeInBottom delay-1s">
                <div class="row">
                    <div class="col-12 mx-2 sm-mx-5 my-4">
                        <p>
                            En naviguant sur le site, vous allez utiliser des cookies pour améliorer votre expérience.
                             (rechagement des données saisies dans les formulaire & popup de messages d'informations & connexion a l'espace membre)
                        </p>
                        <p>
                            Pour continuer à naviguer dans les meilleures conditions vous acceptez l'utilisations des cookies. <a href="/legal#cookies">Plus d'infos.</a>
                        </p>
                        <p>
                            En continuant à naviguer sur le site vous acceptez l'utilisations des cookies.
                        </p>
                    </div>
                    <div class="col-12">
                        <div class="row">
                            <form method="POST" class="bubbleDashboard bubbleDashboardButton col-6 text-center" id="cookiesAccept">
                                <div class="row d-flex justify-content-center">
                                    <button type="submit" formaction="/acceptCookies" class="bg-transparent border-0 mx-2">
                                        <span class="mt-2 mb-2">Accepter les cookies</span>
                                        <i class="fas fa-cookie-bite mt-0 mb-2 ml-3"></i>
                                    </button>
                                </div>
                            </form>
                            <form method="POST" class="bubbleDashboard bubbleDashboardButton col-6 text-center" id="cookiesDeny">
                                <div class="row d-flex justify-content-center">
                                    <button type="submit" formaction="/refusedCookies" class="bg-transparent border-0 mx-2">
                                        <span class="mt-2 mb-2">Refuser les cookies</span>
                                        <i class="fas fa-cookie-bite mt-0 mb-2 ml-3"></i>
                                    </button>
                                </div>
                            </form>
                        </div>
                    </div>
                </div>
            </div>
        {% endif %}
    </body>
</html>